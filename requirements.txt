# ── Step 1: YouTube Scraper ───────────────────────────────
google-api-python-client>=2.131.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
youtube-transcript-api>=0.6.2
python-dotenv>=1.0.1

# ── Step 2: Embedding + Vector DB (Gemini) ───────────────
google-genai>=1.7.0       # 1.7+ has a stable API; <1.7 has yanked versions
supabase>=2.15.0          # 2.15+ is compatible with httpx>=0.28 (required by google-genai)
httpx>=0.28.1,<1.0        # pinned explicitly to resolve supabase/google-genai conflict
tiktoken>=0.8.0           # 0.8.0+ has wheels for Python 3.11-3.13
pypdf>=4.2.0

# ── Optional: Whisper fallback ───────────────────────────
# openai-whisper==20231117
# yt-dlp==2024.4.9

# ── Step 3: AI Advisor + Chat UI ─────────────────────────
# (google-genai is already listed above — it handles chat, embeddings, and vision)
streamlit>=1.35.0

# ── New Features (Steps 5+) ───────────────────────────────
twilio>=8.13.0            # SMS/WhatsApp auto-send (optional)
